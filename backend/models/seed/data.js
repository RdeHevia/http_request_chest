const mongoose = require('mongoose');

const request1 = {
  rawRequest: '{"a":1,"b":2,"c":"de"}'
};

const request2 = {
  rawRequest: '{"x":1,"y":2,"z":"de"}'
};

module.exports = {
  request1, request2
};